# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.26

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Settings
pydantic-settings==2.6.1
pydantic[email]==2.10.3

# Auth
# PyJWT 取代 python-jose（後者已停止維護，且有 CVE-2024-33663 algorithm confusion）。
# PyJWT 2.x 預設拒絕 `none` algorithm + 強制 algorithms whitelist，安全 baseline 較高。
PyJWT[crypto]==2.12.1
bcrypt==4.2.1

# External services
resend==2.29.0
firebase-admin==6.6.0
celery==5.4.0
redis==5.2.1

# PDF generation (Module 15 print_batch)
reportlab==4.2.5
svglib==1.5.1
cairosvg==2.7.1
Pillow==11.0.0

# SAM segmentation (Module 04c production sam_refine / sam_weighted)
# - torch ~800MB；sam_vit_b 模型檔 ~375MB 由 Dockerfile RUN curl 下載
# - torch 2.4.1 是 pytest 全 pass 的版本（Phase B 已驗）
# - 注意：本地 Windows 跑 ViT image encoder forward 會 segfault（已知 PyTorch+Windows
#   相容性問題），不影響 Linux 部署環境（Railway / Docker 容器內穩定運作）。
# - cv2 / numpy 相依由 paint-by-number 引擎 import 時帶入；
#   numpy 須 < 2.0（torch 2.4 ABI 限制），cv2 須 < 4.11（搭配 numpy 1.x）
torch==2.4.1
torchvision==0.19.1
segment-anything==1.0
numpy<2.0
opencv-python<4.11

# paint-by-number 引擎依賴（pbn_gen.py 直接 import）
matplotlib
scikit-learn
scipy
shapely
svgwrite
kneed

# Testing
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.24.0
